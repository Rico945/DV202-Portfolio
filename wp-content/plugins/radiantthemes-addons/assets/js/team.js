// RADIANTTHEMES TEAM ELEMENT.
var WidgetRadiantteamHandler = function ($scope, $) {

if (document.querySelector('.team.swiper-container')) {
    var teamSlider = document.querySelector('.team.swiper-container');
    var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
    var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
    var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
    var spacepostSelector = parseInt(teamSlider.dataset.spacer);
   
  var swiper = new Swiper('.team.swiper-container', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,	 pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
});  

}
if (document.querySelector('.team.swiper-container.element-two')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-two');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-two', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,	pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
if (document.querySelector('.team.swiper-container.element-three')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-three');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-three', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,    pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
if (document.querySelector('.team.swiper-container.element-four')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-four');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-four', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,    pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
if (document.querySelector('.team.swiper-container.element-five')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-five');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-five', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,	pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
if (document.querySelector('.team.swiper-container.element-six')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-six');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-six', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,	pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
if (document.querySelector('.team.swiper-container.element-seven')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-seven');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-seven', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,    pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
if (document.querySelector('.team.swiper-container.element-eight')) {
        var teamSlider = document.querySelector('.team.swiper-container.element-eight');
        var mobileItemsSelector = parseInt(teamSlider.dataset.mobileItems);
        var tabItemsSelector = parseInt(teamSlider.dataset.tabItems);
        var desktopItemsSelector = parseInt(teamSlider.dataset.desktopItems);
        var spacepostSelector = parseInt(teamSlider.dataset.spacer);

    var swiper = new Swiper('.team.swiper-container.element-eight', {
    slidesPerView: mobileItemsSelector,
    spaceBetween: spacepostSelector,
    loop: true,	pagination: {            el: '.swiper-pagination',            clickable: true,        },
    breakpoints: {
        640: {
            slidesPerView: mobileItemsSelector,
            spaceBetween: spacepostSelector,
        },
        768: {
            slidesPerView: tabItemsSelector,
            spaceBetween: spacepostSelector,
        },
        1024: {
            slidesPerView: desktopItemsSelector,
            spaceBetween: spacepostSelector,
        },
    }
    });
}
var wobbleElements = document.querySelectorAll('.wobble');

wobbleElements.forEach(function (el) {
    el.addEventListener('mouseover', function () {

        if (!el.classList.contains('animating') && !el.classList.contains('mouseover')) {

            el.classList.add('animating', 'mouseover');

            var letters = el.innerText.split('');

            setTimeout(function () {
                el.classList.remove('animating');
            }, (letters.length + 1) * 50);

            var animationName = el.dataset.animation;
            if (!animationName) {
                animationName = "jump";
            }

            el.innerText = '';

            letters.forEach(function (letter) {
                if (letter == " ") {
                    letter = "&nbsp;";
                }
                el.innerHTML += '<span class="letter">' + letter + '</span>';
            });

            var letterElements = el.querySelectorAll('.letter');
            letterElements.forEach(function (letter, i) {
                setTimeout(function () {
                    letter.classList.add(animationName);
                }, 50 * i);
            });

        }

    });

    el.addEventListener('mouseout', function () {
        el.classList.remove('mouseover');
    });
});

}
jQuery(window).on("elementor/frontend/init", function () {
    elementorFrontend.hooks.addAction(
        "frontend/element_ready/radiant-team.default",
        WidgetRadiantteamHandler
    );
});